name: ronald_dela_cruz_portfolio_v2
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # Core dependencies
  cupertino_icons: ^1.0.8
  provider: ^6.1.1

  # Firebase
  firebase_core: ^3.15.1
  cloud_firestore: ^5.6.11
  #firebase_storage: ^11.6.0

  # Routing (GetX)
  get: ^4.6.6

  # UI Enhancements
  google_fonts: ^6.1.0
  responsive_builder: ^0.7.0
  flutter_animate: ^4.2.0
  url_launcher: ^6.3.2
  shared_preferences: ^2.5.3
  webview_flutter: ^4.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

# Override (optional — only needed for firebase_storage)
dependency_overrides:
  firebase_storage_platform_interface: 5.2.1

flutter:
  uses-material-design: true

  assets:
    - assets/placeholderImage.jpg
    - assets/profilepic.jpg
